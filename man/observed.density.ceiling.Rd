% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions.R
\name{observed.density.ceiling}
\alias{observed.density.ceiling}
\title{The highest density this species was ever actually seen at, by season}
\usage{
observed.density.ceiling(segdata)
}
\arguments{
\item{segdata}{The \code{segdata} saved by the DSM step, with \code{estAbund},
\code{segment.area}, \code{Season} and \code{Sample.Label} columns. An \code{sf} object is
fine; the geometry is dropped.}
}
\value{
A tibble with one row per season: \code{season}, \code{n_seg} (physical
segments, not rows), \code{n_pos}, \code{obs_max}, \code{obs_p999}, \code{obs_p99},
\code{obs_mean}. A season the species was never recorded in gets \code{obs_max} 0,
which callers must treat as "no reference", not as a ceiling of zero.
}
\description{
A prediction-surface sanity check needs a ceiling, and a flat one cannot
serve every species. Measured across \code{NL_EXPL_DRL_RA}, the largest
detection-corrected density on any segment ranges from \strong{0 to 7,183
birds/km2} depending on species and season -- a factor of seven thousand. A
single constant is therefore far too permissive for a razorbill in spring
(whose busiest segment ever held 16 birds/km2) and barely above the
legitimate range for a fulmar in spring (7,183). This returns the
species-and-season-specific reference instead.
}
\details{
What it measures is deliberately the observed \strong{maximum}, not a quantile:
the statement it supports is "the model predicts a seasonal-mean density in
one cell higher than the single busiest instantaneous encounter ever
recorded", which is interpretable without further calibration. \code{p999} and
\code{p99} come back too, because the maximum is one flock and worth seeing beside
a less brittle number.

Predictions should ordinarily sit \strong{well below} this. A prediction cell is a
seasonal mean over \code{predgrid.years}; an observed segment density is one
encounter on one day. Measured over 88 NL_EXPL_DRL_RA surfaces, the median
ratio of largest predicted cell to observed maximum is \strong{0.075} -- the
typical surface peaks thirteen times below anything ever seen.
}
\examples{
\dontrun{
load(here(RDataDir, "ATPU_distdata_segdata_ddfs.Rda"))
observed.density.ceiling(segdata)
}
}
