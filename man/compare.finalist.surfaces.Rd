% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions.R
\name{compare.finalist.surfaces}
\alias{compare.finalist.surfaces}
\title{Compare the prediction surfaces of two models, cell by cell}
\usage{
compare.finalist.surfaces(
  paths1,
  paths2,
  season,
  species,
  model1,
  model2,
  cell.area,
  top_n = 10,
  spike.ratio = 10,
  map.limit = NULL,
  ref.density = NULL,
  ref.mult = 100
)
}
\arguments{
\item{paths1, paths2}{Character vectors of raster files to compare, one per
season, in the same order. Both models must have been predicted on the same
grid; the geometries are checked.}

\item{season}{Season labels, the same length as \code{paths1}.}

\item{species}{Character string species code, carried into the output.}

\item{model1, model2}{Character strings naming the two models.}

\item{cell.area}{Cell area in km^2, i.e. \code{predgridCellArea}. The rasters hold
density, so this is what turns a summed surface into an abundance.}

\item{top_n}{How many of the largest cells to accumulate and to list. Default
10, matching \code{PRED_CONCENTRATION_TOP_N}.}

\item{spike.ratio}{A cell counts as a spike when it exceeds this multiple of
the surface's median positive density. Default 10.}

\item{map.limit}{Optional flat density above which a cell is hidden from the
maps, i.e. \code{MAX_DENS_VALUE}. Worth passing, but for a narrow reason: that
limit is applied only when drawing leaflet maps and never to the rasters
that get shared, so \code{n_over_limit} counts exactly the cells that are
invisible on the map you would check a surface against and fully present in
the file. It is a \strong{visibility} measure, not a plausibility one -- see
\code{ref.density} for that. \code{NULL} leaves \code{n_over_limit} and \code{pct_over_limit}
\code{NA}.}

\item{ref.density}{The highest detection-corrected density ever observed on a
segment, one value per season in the order of \code{season} (a scalar is
recycled). From \code{\link[=observed.density.ceiling]{observed.density.ceiling()}}. This is the
species-and-season-specific ceiling, and it is the right one: across
\code{NL_EXPL_DRL_RA} the observed maximum spans 0 to 7,183 birds/km2 between
species-seasons, so no single constant can be meaningful for all of them.
A season with no observations at all gets no reference rather than a
ceiling of zero. \code{NULL} leaves the \verb{*_obs} columns \code{NA}.}

\item{ref.mult}{How many times \code{ref.density} a cell must exceed to be
counted in \code{n_over_obs}. Default 100, measured rather than picked -- see
the calibration section.}
}
\value{
A list of two tibbles:
\describe{
\item{\verb{$seasons}}{One row per season. Per-model columns are suffixed \code{1}
and \code{2}; \code{total_ratio}, \code{max_ratio} and the correlations are the
pairwise ones. \code{pct_cells_2x} is the share of cells where the two
surfaces differ by more than a factor of two, counting only cells where
at least one model predicts something -- the measure of \emph{where} they
disagree that a correlation cannot give. \code{top_n_shared} is how many of
the two models' \code{top_n} cells are the same cells: whether they are
spiky in the same places, as distinct from equally spiky.
\code{n_over_limit} and \code{pct_over_limit} count cells hidden from the maps;
\code{max_over_obs}, \code{n_over_obs} and \code{pct_over_obs} are the plausibility
check against what was actually observed.}
\item{\verb{$cells}}{The \code{top_n} largest cells of each model, with the other
model's value for the same cell, the coordinates and the ratio. This is
what names the spots -- a caller can look each one up in the
extrapolation assessment, exactly as \code{03.70} does for the chosen
model.}
}
A season whose rasters are missing gets a row with \code{missing_raster} set
rather than being dropped, so the caller can see which comparison did not
happen.
}
\description{
Answers what the family cross-validation and the in-sample diagnostics both
leave open: when neither can separate the Tweedie and the negative binomial
finalist, do their predictions actually differ, and where?
}
\details{
The comparison is deliberately two-sided. \strong{Agreement} is the correlation
between the two surfaces and the ratio of their totals -- if those are tight
the choice of family does not matter for the product, and a tie can be left
as a tie. \strong{Spikiness} is the rest of it, and is the half worth reading
first: two surfaces can correlate at 0.99 and still put wildly different
amounts of the total into a handful of cells, because that correlation is
carried by the tens of thousands of cells where both models predict almost
nothing.

The spike measures are scale-free on purpose (\code{max_over_med}, \code{n_spikes},
\code{gini}), so a family predicting twice the abundance is not thereby recorded
as twice as spiky. Two cautions come with them. The ones divided by
\code{median_pos} go meaningless on a near-empty surface, where that denominator
goes denormal -- \code{NL_EXPL_DRL_RA} RAZO Fall has a median positive cell of
2.0e-13 and so a \code{max_over_med} of 1.5e12 off a maximum of 0.31 birds/km^2 --
which is why \code{median_pos} is returned beside them and should be read with
them. And none of them catches a surface that has blown up \emph{everywhere}:
that is what \code{map.limit} is for.
}
\section{Calibrating the plausibility ceiling}{

\code{max_over_obs} -- the largest predicted cell over the largest density ever
observed on a segment of the same species and season -- separates the
populations that a flat limit cannot. Measured over 88 \code{NL_EXPL_DRL_RA}
surfaces:

\itemize{
\item median \strong{0.075}. The typical surface peaks thirteen times \emph{below}
anything ever seen, which is what should happen: a prediction cell is a
seasonal mean over ten years and an observed segment density is one
encounter on one day.
\item 75th percentile 0.23, 90th 4.2, 95th 16.2. Twelve surfaces exceed 1.
\item The legitimate tail tops out at \strong{32.5} (LESP Winter under \code{nb}),
with Shearwaters Spring at 19.0 and 11.1 -- concentrated, plausible,
flock-driven surfaces.
\item Then a gap of five orders of magnitude to \strong{7.7e6}, Petrels Winter
under \code{tw}.
}

\code{ref.mult = 100} sits in that gap: three times above the worst legitimate
surface and four orders of magnitude below the pathology. It is a wide gap,
so the exact value is not delicate -- anything from 50 to 1,000 separates the
same two sets.

Compare what the flat \code{MAX_DENS_VALUE} of 10,000 does on the same data. It
fires on 3 of 88 surfaces, and it cannot do better, because it is set near
the \emph{top} of the observed range: real segments have held 7,183 birds/km2
(NOFU Spring), 4,091 (BLKI Fall) and 2,734 (BLKI Winter). For a razorbill in
spring, whose busiest segment ever held 16 birds/km2, a predicted cell of
5,000 would be three hundred times anything ever recorded and the flat rule
would say nothing at all.
}

\section{What this found on NL_EXPL_DRL_RA}{

Petrels Winter, fitted with \code{tw()} and the model \code{final.dsm.models} names, so
the surface \code{03.70} ships: \strong{2,267 cells above \code{MAX_DENS_VALUE}}, together
holding 99.9997\% of the seasonal total, peaking at 1.7e10 birds/km^2 for a
seasonal total of 3.95e12 birds. The \code{nb()} finalist on the same segments and
the same grid gives 6.3e6. The two surfaces correlate at 0.037.

The existing concentration check does not see it. \code{pct_top_n} for that
surface is 14.9\%, far below the level \code{03.70} warns on, because the blow-up
is spread over thousands of cells instead of piled into one -- and
\code{MAX_DENS_VALUE} is applied only when drawing maps, so every one of those
cells is absent from the picture and present in the file. A concentration
measure asks whether one cell dominates; \code{n_over_limit} asks whether many
cells are impossible, and only the second question had a useful answer here.

Concentration is where the response family stops being a detail, which is why
this exists: where one observation dominates, Tweedie and negative binomial
disagree about how likely a huge count is, and the totals diverge
accordingly. See \code{\link[=summarise.prediction.concentration]{summarise.prediction.concentration()}} for the \verb{Atl IMRP}
Herring Gull case that established the pattern.
}

\examples{
\dontrun{
compare.finalist.surfaces(
  paths1 = file.path(predDir,
    sprintf("ATPU.\%s.dsm_tw_allpred_abund_factor.4_sqkm.tif", season.names)),
  paths2 = file.path(predDir,
    sprintf("ATPU.\%s.dsm_nb_allpred_abund_factor.4_sqkm.tif", season.names)),
  season = season.names, species = "ATPU",
  model1 = "dsm_tw_allpred_abund_factor",
  model2 = "dsm_nb_allpred_abund_factor",
  cell.area = predgridCellArea, map.limit = MAX_DENS_VALUE)
}
}
