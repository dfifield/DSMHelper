% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions.R
\name{get.what.counts}
\alias{get.what.counts}
\title{Augment a transect row with WhatCount values from its constituent watches}
\usage{
get.what.counts(row, watches)
}
\arguments{
\item{row}{Single-row data frame for one transect, with a \code{Watches}
column of comma-separated watch IDs.}

\item{watches}{Data frame of watch records with \code{WatchID} and
\code{WhatCount} columns.}
}
\value{
\code{row} with \code{WhatCounts} (list column of unique values)
and \code{nWhatCount} (integer count) appended.
}
\description{
For each row in a transects data frame, determines how many distinct
\code{WhatCount} values appear across the watches it aggregates and stores
both the values and their count.  Note: performance is poor when called via
\code{map()} on large data frames.
}
