% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions.R
\name{check.problem.data}
\alias{check.problem.data}
\title{Flag watches with suspicious GPS positions}
\usage{
check.problem.data(
  alldat,
  rel.folder,
  leave = NULL,
  fixed_db = NULL,
  quiet = TRUE,
  filename = "suspicious_posns.csv",
  ...
)
}
\arguments{
\item{alldat}{Data frame of all watch data.}

\item{rel.folder}{Path relative to \code{here()} where the output CSV is
written.}

\item{leave}{Optional vector of watch IDs to exclude from flagging; passed
to \code{ECSAS.find.suspicious.posn()}.}

\item{fixed_db}{Optional character vector of watch IDs already recorded as
fixed in the database.}

\item{quiet}{If \code{TRUE} (default), suppresses printing of problem IDs
and diagnostic histograms.}

\item{filename}{Name of the output CSV file.}

\item{...}{Additional arguments passed to
\code{ECSAS.find.suspicious.posn()}.}
}
\value{
Data frame of suspicious watches; a 0-row data frame if none were
found (previously \code{NULL}, which made callers such as
\code{table(probs$Program)} fail with "nothing to tabulate").
}
\description{
Wraps \code{ECSAS.find.suspicious.posn()} and annotates results with
whether each flagged watch was previously recorded as fixed in the
database.  Results are written to a CSV in \code{rel.folder}.
}
