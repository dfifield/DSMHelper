% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions.R
\name{plot_covar}
\alias{plot_covar}
\title{Plot a covariate against distance categories}
\usage{
plot_covar(
  covar = NULL,
  distdata = NULL,
  species = NULL,
  df.spec = NULL,
  suffix = ""
)
}
\arguments{
\item{covar}{Character string name of the covariate column to plot.}

\item{distdata}{Data frame of observation data.}

\item{species}{Character string species code; used in the plot title.}

\item{df.spec}{DDF specification list supplying cutpoints and distance
centres.}

\item{suffix}{Character string appended to the plot title.}
}
\value{
\code{invisible(NULL)}, called for its side-effect (plot).
}
\description{
Produces a boxplot (for \code{"size"}), a violin plot (for factor
covariates), or a scatter plot with a linear smoother (for continuous
covariates).
}
\details{
Named with an underscore rather than the project's usual dot separator:
\code{plot} is an S3 generic, so a function called \code{plot.covar} is
registered by roxygen as a \code{plot} method for the (non-existent) class
\code{"covar"} instead of being exported, and is then not callable by name.
This takes a covariate \emph{name}, not an object, so it is not a method.
}
