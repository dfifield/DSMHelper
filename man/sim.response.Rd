% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions.R
\name{sim.response}
\alias{sim.response}
\title{Draw from a fitted model's predictive distribution}
\usage{
sim.response(fit, mu, n_sim)
}
\arguments{
\item{fit}{Fitted \code{bam}/\code{gam} object.}

\item{mu}{Numeric vector of fitted means on the response scale.}

\item{n_sim}{Number of draws per element of \code{mu}.}
}
\value{
Numeric matrix, \code{length(mu)} rows by \code{n_sim} columns.
}
\description{
Simulates new responses at supplied fitted means using the fitted family's own
parameters. Add a branch here when a new family is added to
\code{dsm.mod.specs}; the error is deliberate rather than a silent fallback,
because a wrong predictive distribution would quietly corrupt every score.
}
\examples{
\dontrun{
sim.response(fit, predict(fit, type = "response"), 200)
}
}
