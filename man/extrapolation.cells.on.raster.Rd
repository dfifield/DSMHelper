% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions.R
\name{extrapolation.cells.on.raster}
\alias{extrapolation.cells.on.raster}
\title{Map a dsmextra result onto the geometry of a prediction raster}
\usage{
extrapolation.cells.on.raster(ex, pred.raster, covariate.names)
}
\arguments{
\item{ex}{A \code{dsmextra} extrapolation object with a \verb{$rasters} element.}

\item{pred.raster}{\code{SpatRaster} defining the target geometry.}

\item{covariate.names}{Character vector used to resolve the integer MIC
index to a covariate name.}
}
\value{
A tibble with \code{cell}, \code{x}, \code{y}, \code{ExDet}, \code{type}, \code{mic_name}. \code{type}
is \code{"univariate"}, \code{"analogue"}, \code{"combinatorial"}, or \code{NA} where
\code{dsmextra} placed no value.
}
\description{
Resamples the ExDet and MIC rasters returned by
\code{\link[dsmextra:compute_extrapolation]{dsmextra::compute_extrapolation()}} onto \code{pred.raster} and returns one row
per non-\code{NA} cell of that raster. See the \emph{Why the result is resampled}
section of \code{\link[=assess.extrapolation]{assess.extrapolation()}} for why a coordinate join does not work.
}
