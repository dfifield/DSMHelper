% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions.R
\name{get.seas.mean}
\alias{get.seas.mean}
\title{Compute seasonal means for all dynamic variables for one season}
\usage{
get.seas.mean(seas, season.spec, dat, dyn.vars)
}
\arguments{
\item{seas}{Character string season label.}

\item{season.spec}{Named list of season boundary definitions for the
species (from the project \code{seasons} list).}

\item{dat}{Data frame (normally a prediction grid) with monthly covariate
columns and a \code{Season} column.}

\item{dyn.vars}{Character vector of dynamic variable names (e.g.
\code{c("sst", "sst.g")}).}
}
\value{
\code{dat} (filtered to \code{seas}) with seasonal mean columns
appended.
}
\description{
Filters \code{dat} to rows matching \code{seas}, then calls
\code{\link{get.seas.mean.var}} for each variable in \code{dyn.vars} to
compute the mean over the months defined in \code{season.spec}.
}
\details{
The month set comes from \code{\link{season.months}}, which handles a season
that wraps the year end. This function used to derive it here as
\code{start:end}, which for Winter is \code{11:3} and counts downwards --
see \code{\link{season.months}} for what that cost (issue #74).
}
