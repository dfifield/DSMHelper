% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions.R
\name{impute.missing.perp.distances}
\alias{impute.missing.perp.distances}
\title{Impute a distance for observations that should have had one}
\usage{
impute.missing.perp.distances(distdata)
}
\arguments{
\item{distdata}{Data frame with \code{distance}, \code{DistType}, \code{SurveyType},
\code{FlySwim} and \code{DistMeth} columns. \code{distbegin}/\code{distend} are used if present.}
}
\value{
\code{distdata} with imputable rows filled in. Rows whose \code{DistType} is not
\code{"Perp."}, and rows in a group with no observed distances to average, are
returned unchanged.
}
\description{
There are two ways an observation ends up without a perpendicular distance,
and they mean opposite things.
}
\details{
The one that matters is \strong{by design}: the watch's \code{DistMeth} says no
perpendicular distances are taken for that behaviour - \code{lkpDistMeth} gives
flying birds \code{None} under DistMeth 1 and 13 and \code{Radial} under 17 and 19, and
DistMeth 17 was standard from mid-2008 to the end of 2011. Those observations
are separate survey effort, they belong in the \verb{_N} (strip/dummy) ddf, and
this function leaves them alone.

The other is an \strong{observer omission}: the DistMeth says perpendicular
distances are being recorded, and one simply was not. Those did not happen on
separate effort, so putting them in the \verb{_N} ddf misattributes them. They used
to be swept up by a blanket \code{is.na(distance) ~ 0} fill, which is worse than
either option - it fabricates a detection exactly on the trackline, biasing
the detection function steeply toward zero distance.

They are rare enough that the choice barely matters in aggregate - 8 rows of
47,307 perpendicular observations on NL_EXPL_DRL_RA, 0.017\% - but "rare" is
not "absent" and a silent zero is not a defensible value. Each gets the mean
observed distance for its own (\code{SurveyType}, \code{FlySwim}, \code{DistMeth}) group,
snapped to the nearest distance actually observed in that group.

The snapping matters because these data are binned: ship observations carry
the bin centre in \code{distance} (0.025, 0.075, 0.15, 0.25 km) and have \code{distbegin}
/ \code{distend} NA, since the binning happens at ddf-fitting time from the spec's
\code{cutpoints}. Assigning a raw group mean would put a value on the pile that is
not a bin centre; snapping keeps every distance one of the values the survey
can actually produce. Where \code{distbegin}/\code{distend} ARE populated (the aerial
path), the pair belonging to the chosen distance is copied across too.

Deterministic - no RNG, so a rerun gives the same answer.
}
