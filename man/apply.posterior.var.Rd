% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions.R
\name{apply.posterior.var}
\alias{apply.posterior.var}
\title{Apply posterior sampling to one chunk of prediction cells}
\usage{
apply.posterior.var(
  dat,
  this.dsm,
  Bt,
  logf,
  K,
  probs,
  draw.block,
  agg = NULL,
  agg.levels = NULL
)
}
\arguments{
\item{dat}{Data frame for one chunk, already geometry-dropped and ordered
cell-major: \code{K} consecutive rows per cell, cells in output order.
Carries \code{.my.off.set}.}

\item{this.dsm}{Fitted \code{dsm}/\code{gam} object.}

\item{Bt}{Coefficient draws, \code{p x B}, transposed once by the caller so
no chunk pays for it.}

\item{logf}{Length-\code{B} vector of log detection-function factors, all
zero when that term is off.}

\item{K}{Platform copies per cell; constant by construction, asserted by the
caller.}

\item{probs}{Quantile probabilities.}

\item{draw.block}{Draws evaluated per matrix multiply.}

\item{agg}{Character vector of length \code{nrow(dat)/K} labelling the
aggregate each cell belongs to, or \code{NULL}.}

\item{agg.levels}{Full level set for \code{agg}, so every chunk returns rows
in the same order and chunks can simply be added.}
}
\value{
List with \code{cells} (one row per cell) and \code{agg.draws},
carrying a \code{chunk.stats} attribute.
}
\description{
Worker for \code{\link{get.per.cell.var.posterior}}. Builds one
\code{lpmatrix} for the whole chunk, evaluates the posterior draws against it
in blocks, sums the platform copies of each cell \emph{inside} each draw, and
summarises across draws.
}
