% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions.R
\name{create.ncdf.rast}
\alias{create.ncdf.rast}
\title{Create a raster from one time slice of a NetCDF array}
\usage{
create.ncdf.rast(
  the.date,
  index,
  dat,
  datname,
  x,
  y,
  inproj,
  outproj = NULL,
  to = NULL
)
}
\arguments{
\item{the.date}{Date string labelling this layer (used in the plot title
and passed as the raster name); use \code{""} for static datasets.}

\item{index}{Integer index of the time slice to extract from \code{dat}.}

\item{dat}{3-D numeric array with dimensions (lon, lat, time), as returned
by \code{ncdf4::ncvar_get()}.}

\item{datname}{Character string variable name (e.g. \code{"sst"}); used
in the plot title.}

\item{x}{Numeric vector of longitude coordinates.}

\item{y}{Numeric vector of latitude coordinates.}

\item{inproj}{Projection string or EPSG code for the input data.}

\item{outproj}{Target projection for reprojection; ignored when \code{to}
is supplied.}

\item{to}{Optional \code{SpatRaster} template; if supplied the result is
reprojected, resampled, and masked to match it.}
}
\value{
A \code{SpatRaster} for the requested time slice.
}
\description{
Extracts the layer at position \code{index} from \code{dat}, flips the
y-dimension (which is inverted in NetCDF convention), optionally reprojects
or resamples to match \code{to}, and plots the result.
}
