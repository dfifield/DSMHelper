% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/DSMHelper.R
\name{do.det.fcn.spec}
\alias{do.det.fcn.spec}
\title{Fit detection function(s) for one DDF spec and species}
\usage{
do.det.fcn.spec(
  df.spec,
  df.spec.nm,
  species,
  distdata,
  do.final.only,
  rerun,
  parallel,
  nCores,
  do.eda,
  ...
)
}
\arguments{
\item{df.spec}{A single DDF specification list.}

\item{df.spec.nm}{Name of the spec, e.g. \code{"ECSAS_Ship_W_D"}.}

\item{species}{Character string species code.}

\item{distdata}{Data frame of observation data (all species; filtered
internally).}

\item{do.final.only}{If \code{TRUE}, fit only the final model and store it
in \code{df.spec}.}

\item{rerun}{If \code{TRUE}, re-fit even when saved results exist.}

\item{parallel}{If \code{TRUE}, use parallel processing.}

\item{nCores}{Number of cores for parallel processing.}

\item{do.eda}{If \code{TRUE}, produce EDA plots.}

\item{...}{Additional arguments passed to \code{\link{do.det.fcn}}.}
}
\value{
Updated \code{df.spec} with \code{fitted.model},
\code{fitted.distdata}, and \code{ddftype} added when
\code{do.final.only = TRUE}.
}
\description{
Filters \code{distdata} for the appropriate dataset, platform class,
behaviour, and distance type; runs EDA plots; then calls
\code{\link{do.det.fcn}} to fit either all candidate detection functions or
just the pre-selected final model.  Stores the fitted model and augmented
distdata back into \code{df.spec} when \code{do.final.only = TRUE}.
}
\details{
Expects project globals \code{spec.grps}, \code{seasons},
\code{ResultsDir}, and \code{ddftype_levels} in the calling environment.
}
