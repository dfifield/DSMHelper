% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions.R
\name{write.finalist.summary.page}
\alias{write.finalist.summary.page}
\title{Write the shareable summary page for the finalist prediction comparison}
\usage{
write.finalist.summary.page(
  seasons,
  cells,
  path,
  subproject,
  cell.km2,
  top_n = 10,
  ref.mult = 100,
  map.limit = NULL,
  warn.pct = 50
)
}
\arguments{
\item{seasons}{The roll-up tibble: one row per species-season, as
\code{\link[=compare.finalist.surfaces]{compare.finalist.surfaces()}} returns them, bound across species, with an
\code{incumbent} column added.}

\item{cells}{The matching spike-cell tibble.}

\item{path}{Where to write the file.}

\item{subproject}{SubProject name, for the masthead.}

\item{cell.km2}{Prediction cell area, for the masthead.}

\item{top_n, ref.mult, map.limit}{The settings the numbers were computed under,
so the page states its own thresholds instead of leaving them implicit.}

\item{warn.pct}{The concentration level the shipping step warns at.}
}
\value{
\code{path}, invisibly.
}
\description{
The per-species reports and the roll-up answer the question in detail; this
is the one page to hand someone who was not in the room. Everything on it is
computed from the arguments -- no finding, figure or species name is written
into the template -- so it stays true after a refit, and a SubProject where
nothing is wrong gets a page that says so rather than an empty version of
the page where something was.
}
\details{
The output is a standalone HTML document that opens on its own. The body is
delimited by \verb{<!-- ARTIFACT:BEGIN -->} and \verb{<!-- ARTIFACT:END -->} sentinels,
because publishing it as an Artifact needs the content without the
\verb{<!doctype>}/\verb{<head>}/\verb{<body>} scaffold, which that publisher supplies
itself. Extract between the sentinels rather than re-deriving the page.
}
\examples{
\dontrun{
write.finalist.summary.page(seasons, cells,
  file.path(ResultsDir, paste0(SubProject, "_finalist_comparison.html")),
  subproject = SubProject, cell.km2 = predgridCellArea)
}
}
