% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions.R
\name{do.det.fcn}
\alias{do.det.fcn}
\title{Fit detection function(s) for one dataset/species combination}
\usage{
do.det.fcn(
  distdata,
  species,
  df.model = NULL,
  df.model.nm = NULL,
  dsetname = NULL,
  do.final.only = TRUE,
  cleanFolder = FALSE,
  rerun = FALSE,
  parallel = FALSE,
  folder,
  nCores = 1,
  ...
)
}
\arguments{
\item{distdata}{Data frame of observation data (already filtered for the
relevant species, behaviour, and platform).}

\item{species}{Character string species code.}

\item{df.model}{DDF specification list; used when \code{do.final.only =
TRUE}.}

\item{df.model.nm}{Name of the DDF spec (e.g. \code{"ECSAS_Ship_W_D"}).}

\item{dsetname}{Character string dataset name.}

\item{do.final.only}{If \code{TRUE} (default), fit only the final model.}

\item{cleanFolder}{If \code{TRUE}, remove existing results from
\code{folder} before fitting (only used when \code{do.final.only =
  FALSE}).}

\item{rerun}{If \code{TRUE}, re-fit even when saved results exist.}

\item{parallel}{If \code{TRUE}, use parallel processing.}

\item{folder}{Path to the folder where candidate model summaries are saved.}

\item{nCores}{Number of cores for parallel processing.}

\item{...}{Additional arguments passed to \code{\link[Distance]{ds}}.}
}
\value{
When \code{do.final.only = TRUE}: named list with elements
\code{df_final} (fitted model) and \code{distdata} (augmented with
\code{detProb} and \code{adjSize}).  When \code{do.final.only = FALSE}:
return value of \code{\link{do.ds}}.
}
\description{
Either fits the single pre-selected final detection function
(\code{do.final.only = TRUE}) or runs all candidate models via
\code{\link{do.ds}} (\code{do.final.only = FALSE}).  When fitting the
final model, checks it with \code{\link{check.det.fcn}} and returns
augmented distdata.
}
